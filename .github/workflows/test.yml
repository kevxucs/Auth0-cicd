name: Test
on: 
  push:
jobs:
  deploy:
    runs-on: ubuntu-latest
    
    if: github.ref == 'refs/heads/master'
    # needs:
    #   - build
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2-beta
        with:
          node-version: "12"
      - name: NPM Install
        run: npm i -g auth0-deploy-cl